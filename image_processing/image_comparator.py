import imageio
from image_processing.tile_image import TileImage


def get_simple_difference(image1: TileImage, image2: TileImage):
    for i in range(len(image1.data)):
        for j in range(len(image1.data[i])):
            pass

    pass
